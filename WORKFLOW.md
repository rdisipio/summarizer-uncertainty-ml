# Workflow: Laplace-over-LoRA per-sentence uncertainty for summaries

> Purpose: instruct a generative-AI code assistant to implement a lean, production-capable application that produces **per-sentence epistemic uncertainty** derived *only* from a Laplace approximation over LoRA adapter weights for a seq2seq summarizer. This file specifies the architecture, data formats, training, Laplace fitting, inference scoring, API surface, tests, and deployment notes.

---

## 1. High-level overview

**Goal:** Given a source document `source` and a single *displayed summary* `summary` (already generated by any LLM), produce a per-sentence uncertainty score that comes *only* from a Laplace posterior over LoRA adapter parameters trained on a seq2seq base model (e.g., BART/T5).

**Constraints / design choices**:
- The generator (LLM) is external / model-agnostic. We will _not_ rely on multiple generated summaries or clustering.
- Uncertainty is epistemic and comes solely from sampling LoRA weights under a Laplace approximation.
- Use PEFT/LoRA for cheap adapters. Fit Laplace on adapter parameters only.
- Evaluate a displayed summary by *scoring the exact sentence tokens* under sampled adapter weights (teacher-forcing / label scoring), preserving the summary prefix that precedes each sentence.
- Inference is a post-hoc scoring pass over an existing summary, not a re-generation step.

---

## 2. Components & repo layout

```
repo-root/
├─ data/                     # training / calibration / small eval sets
├─ src/
│  ├─ train.py               # LoRA training pipeline
│  ├─ laplace.py             # Laplace-fitting utilities
│  ├─ sampler.py             # draws LoRA samples and injects into model
│  ├─ scorer.py              # scoring (per-token log-probs) + aggregation to sentence score
│  ├─ api_server.py          # minimal REST API for score requests
│  ├─ utils.py               # tokenization, model-loading helpers
│  └─ config.py              # hyperparams and constants
├─ notebooks/                # optional experiments / quick demos
├─ tests/                    # unit + integration tests
├─ requirements.txt
└─ workflow.md               # this file
```

---

## 3. Dependencies

- Python 3.10+
- PyTorch (>=1.12)
- transformers (Hugging Face)
- datasets (Hugging Face) optional for training
- peft (for LoRA adapters)
- numpy, scipy
- tqdm
- scikit-learn (for minor utils)
- opt: `laplace` library or small custom Laplace impl; if not available implement diagonal Laplace directly
- FastAPI + uvicorn (for API)

Install example (pip):

```bash
pip install torch transformers datasets peft numpy scipy tqdm scikit-learn fastapi uvicorn
```

---

## 4. Data formats

**Training data (for LoRA):** a tabular dataset with columns:
- `id`: unique id
- `source`: full source text
- `summary`: target summary text (one or multi-sentence) — used to train seq2seq LoRA

Tokenization and padding handled in training script. Save data as JSONL or `datasets` format.

**Calibration dataset (small human-labeled):** CSV/JSONL with rows:
- `id`, `source`, `summary`, `sentence_index`, `sentence_text`, `human_uncertainty` (0..1 scale)

This is used only for mapping raw Laplace-derived metrics into human-calibrated scores.

**Inference input (API):** JSON
```json
{ "source": "...", "summary": "...", "sentences": ["..."] }
```
`sentences` can be derived by the client or server; server should support both.

API response:
```json
{
  "sentence_results": [
    {"sentence_index": 0, "uncertainty": 0.31, "mean_logprob": -1.24, "epistemic_mi": 0.028, "predictive_entropy": 1.44},
    ...
  ]
}
```

---

## 5. Training LoRA (train.py)

**Purpose:** produce a MAP checkpoint of LoRA adapter parameters on a pre-trained seq2seq base (BART or T5).

Key steps:
1. Load base model and tokenizer (`facebook/bart-large-cnn` recommended for English news-style summarization).
2. Add LoRA adapters using `peft.get_peft_model` with config: `r=8..16`, `alpha=32`, `dropout=0.1`. Target modules: decoder `q_proj,k_proj,v_proj,o_proj` (names vary by model).
3. Freeze base model parameters, set LoRA adapters + head as trainable.
4. Use teacher-forcing cross-entropy training with labels (standard seq2seq). Save: base config + LoRA weights (PEFT saving pattern).

Output:
- `lora_map_checkpoint/` — contains adapter tensors; also keep `train_dataset_hash.txt`.

Hyperparams (suggested):
- epochs: 3–5
- batch size: 4–16 (depending on GPU)
- learning rate: 1e-4 to 3e-4
- optimizer: AdamW, weight decay 0.01

Note: training script should save a flat vectorized copy of adapter params for Laplace fitting convenience (see laplace.py).

---

## 6. Laplace fitting (laplace.py)

**Goal:** compute a Laplace approximation over the LoRA parameter vector θ (adapter params only) at MAP.

Start with **Diagonal Laplace** (fast):
1. Extract flat parameter vector θ̂ from LoRA adapters.
2. Compute diagonal of the empirical Fisher (or diagonal Hessian approx) using a held-out subset or training data: accumulate grads of negative log-likelihood (NLL) per parameter and average squared gradients -> diag(F) ≈ E[g_i^2].
3. Convert to covariance diagonal: `var_i = 1 / (diag_F_i + λ)` with damping λ (e.g., 1e-3).
4. Save θ̂ and `var` vector to disk as `laplace_diag.npz`.

Optional: K-FAC or block-diagonal Laplace later (laplace.py should be structured to be swappable).

APIs exposed by `laplace.py`:
- `fit_diagonal_laplace(model, data_loader, adapter_param_names, damping) -> (theta_hat, var_diag)`
- `load_laplace(path) -> LaplacePosterior` (object with `.sample()`)

---

## 7. Sampler & injection (sampler.py)

**Responsibilities:** sample S adapter parameter vectors θ_s from N(θ̂, diag(var)) and inject them into the in-memory PEFT model efficiently.

Key points:
- Represent adapter params with a deterministic mapping between flat vector offsets and actual tensors (names and shapes). Save this mapping during training.
- `LaplacePosterior.sample(seed=None)` returns flat vector; `inject_params(flat_vec, model)` writes into adapter tensors in-place (no model reload).
- Provide batched sampling/injection to parallelize forward passes (if memory allows) — e.g., group samples into mini-batches of size B and run scoring for B samples concurrently.

---

## 8. Scoring logic (scorer.py)

**Function:** `score_summary(source, summary, sentences=None, S=40)` -> returns per-token and aggregated per-sentence stats.

Algorithm:
1. Split `summary` into sentences if `sentences` is not provided.
2. Tokenize `source` once for the encoder.
3. Tokenize the full `summary` once and maintain a mapping from each sentence to its decoder token span.
4. For each sentence `i`, score its tokens **with the preceding summary tokens as decoder prefix context**:
   - decoder prefix = all summary tokens before sentence `i`
   - labels = tokens belonging to sentence `i`
   - do **not** score the sentence in isolation unless running an ablation
5. For `s` in `1..S`:
   - sample θ_s
   - inject θ_s into model
   - run model forward with teacher forcing over the decoder prefix + sentence tokens
   - store token-level logits or log-probs for the sentence-token positions only
6. After `S` runs, compute per-token predictive statistics across posterior samples:
   - mean log-prob
   - predictive entropy
   - expected entropy
   - epistemic mutual information `MI = predictive_entropy - expected_entropy`
7. Aggregate token-level uncertainty to a sentence score:
   - default: `sentence_uncertainty = mean(token_mi)`
   - robust option: mean of top-k token MI values to avoid washing out a localized uncertain phrase
8. Return structured stats for each sentence.

**Performance tips**:
- Use `torch.no_grad()` for scoring.
- Use FP16 if supported.
- Batch multiple samples per forward if memory allows by stacking model copies? (Prefer in-place injection loop to avoid memory blowup.)
- Reuse tokenized encoder inputs and full-summary decoder inputs across all sentence spans within the same request.

**Why summary-prefix scoring matters**:
- Seq2seq summarizers are trained autoregressively. The probability of sentence `i` depends on earlier decoder tokens, so uncertainty for sentence `i` should be measured conditional on the already displayed summary prefix.
- Scoring each sentence independently would confound epistemic uncertainty with an artificial context mismatch introduced at inference time.

---

## 9. Calibration (optional but recommended)

Collect a small human-labeled calibration set. Fit a monotonic regressor (isotonic regression or simple linear scaling) that maps raw epistemic features such as sentence-level `mean_token_mi`, `topk_token_mi`, or `mean_logprob` to a human `uncertainty` label (0..1). Save scaler model and apply at inference.

---

## 10. API server (api_server.py)

Expose endpoints:
- `POST /score` — body: `{source, summary, sentences?}` → returns `sentence_results` as described in §4.

Endpoint behavior:
- If `sentences` omitted, server splits `summary` into sentence tokens using a simple sentencizer.
- The server loads base model + LoRA MAP + Laplace posterior on startup to avoid re-loading per request.
- For each request, run `scorer.score_summary` across the summary sentence spans and return aggregated results.

Concurrency: set `max_workers` small; scoring is compute-heavy — consider a queue or rate-limiting.

---

## 11. Tests

- Unit tests for `sampler.inject_params` (inject -> read tensors -> compare to expected slices).
- Unit tests for Laplace fitting on synthetic model (small MLP) to verify diagonal fisher inversion.
- Integration test: small dummy model + tiny dataset; run training (1 epoch), fit Laplace diag, sample S=4, score a short sentence; assert outputs have expected shapes and finite numbers.

---

## 12. Metrics & validation

- Basic sanity: per-token mean logprob should be finite and reasonable (e.g., not all -inf).
- Posterior disagreement stability: increasing S should reduce estimator noise. Validate that sentence-level MI or entropy estimates converge.
- Correlation with human calibration set: Spearman correlation between calibrated score and human label.
- Calibration metrics: RMSE and ECE after mapping.

---

## 13. Deployment notes

- Save artifacts: base model id, LoRA MAP checkpoint, Laplace diag (`theta_hat`, `var_diag`), calibration scaler.
- For low-latency production, consider lowering S (20) or caching pre-tokenized request artifacts.
- Optionally, expose a batch endpoint to score many summaries in one request to amortize encoder work.

---

## 14. Next tasks for the code assistant (prioritized)

1. Implement `src/utils.py` and `src/config.py` with model selection, adapter module-name mapping helper.
2. Implement `train.py` to produce LoRA MAP adapter checkpoint and a flat-parameter dump + name/shape map.
3. Implement `laplace.py` (diagonal Laplace) using squared-gradient Fisher estimator.
4. Implement `sampler.py` with `LaplacePosterior` and `inject_params`.
5. Implement `scorer.py` that performs summary-prefix-aware S-sample scoring and outputs per-sentence stats.
6. Implement `api_server.py` wrapping scorer and adding endpoints + simple rate limiting.
7. Add tests in `tests/` and a brief notebook demo in `notebooks/`.

---

## 15. Minimal acceptance criteria

- End-to-end run: given trained LoRA MAP + Laplace file, API returns a JSON with per-sentence `uncertainty` and `mean_logprob` for each sentence, values finite and normalized.
- Unit tests pass.
- Saved artifacts (MAP + Laplace) load and sampling produces variation across samples.

---

## 16. Appendix: implementation hints

- Use `peft`'s saved adapter named parameters to build the flat parameter vector (flatten and concatenate `A` and `B` matrices in a deterministic order).
- For diagonal fisher, compute `E[g^2]` by running a held-out batch in training mode and accumulating squared gradients per adapter parameter, average across examples.
- Damping `λ` is important: try `1e-3` then tune.
- Keep the sentence-to-token-span mapping explicit so that decoder-prefix conditioning is reproducible and testable.
- If you later want richer posteriors, replace diagonal Laplace by block-K-FAC; keep `LaplacePosterior` API identical.

---

*End of workflow.md*
